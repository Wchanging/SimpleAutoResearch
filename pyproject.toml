[project]
name = "simple-autoresearch"
version = "0.1.0"
description = "A teaching-first lightweight auto-research pipeline inspired by AutoResearchClaw."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arxiv>=3.0.0",
    "openai>=2.34.0",
    "python-dotenv>=1.2.2",
    "scikit-learn>=1.8.0",
    "tqdm>=4.67.3",
]

[project.scripts]
simple-ar = "simple_ar.cli:main"
simple-ar-checks = "simple_ar.dev_checks:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/simple_ar"]
