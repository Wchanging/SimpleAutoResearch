# Claude Code External Backend Profile

Use this profile only inside a SimpleAutoResearch handoff workspace.

Rules:
- Follow the handoff instructions and the permission policy exactly.
- Keep changes scoped to the declared workspace.
- Write canonical artifacts requested in `expected_outputs.json`.
- Avoid global skill installation unless the user explicitly approved it.
- Leave enough provenance for SimpleAutoResearch to review, validate, and replay decisions.
