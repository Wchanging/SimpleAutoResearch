# Reliable Agents

Validation checks can expose invalid tool calls before they affect downstream
work. A small execution benchmark can measure accuracy, failure recovery, and
runtime under the same task contract.

## Evidence

The fixture is intentionally short. It is only a deterministic local input for
the `simple-ar research-brief` example, not a research claim or benchmark
result.
